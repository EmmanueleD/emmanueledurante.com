---
interface Props {
  href: string;
  class?: string;
  ariaLabel?: string;
}

const { href, class: className, ariaLabel } = Astro.props;
---

<a class:list={['text-link', className]} href={href} aria-label={ariaLabel}>
  <slot />
</a>
